import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'drag.drop',
  appName: 'drag-drop',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
